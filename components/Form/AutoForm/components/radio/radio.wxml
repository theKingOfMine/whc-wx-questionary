<view class="radio-card">
  <view class="title-box">
    {{info.label}}
  </view>
  <t-radio-group t-class="theme-card">
    <t-radio wx:for="{{info.selectList}}" wx:key="index" label="{{item.label}}" value="{{item.value}}" />
  </t-radio-group>
</view>